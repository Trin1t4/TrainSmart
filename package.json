{
  "name": "trainsmart",
  "version": "4.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "cd packages/web && npm run dev",
    "build": "cd packages/web && npm install && npm run build",
    "build:web": "cd packages/web && npm run build",
    "dev:api": "echo 'API handled by Vercel'",
    "mobile": "cd packages/mobile && npm start",
    "mobile:android": "cd packages/mobile && npm run android",
    "mobile:ios": "cd packages/mobile && npm run ios"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "pg": "^8.16.3"
  }
}
